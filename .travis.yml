language: python
python:
  - "3.5"
install:
  - "pip install -r requirements.txt"
  - "pip install -r tests/.requirements.txt"
script:
    - "scripts/run-tests.sh"
after_success:
  - "coveralls"

branches:
  only:
    - master
